#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print alphabet in lowercase then in uppercase
 *
 * Return: Always 0 (Success)
*/

int main(void)

{
	int digit1 = 0;
	int digit2, digit3;
	while (digit1 <= 9)
	{{ 
	putchar('\n');

	return (0);
}
